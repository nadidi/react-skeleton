var React = require('react');
var ReactDOM = require('react-dom');
var List = require('./components/List.jsx');
var Header = require('./components/Header.jsx');

ReactDOM.render(<Header />, document.getElementById('ingredients'));
