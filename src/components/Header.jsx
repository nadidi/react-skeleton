var React = require('react');

var Header = React.createClass({
  render: function() {
    retrun (
      <div class="header">
        <h1>Having fun with Raect</h1>
      </div>
    );
  }
});

module.exports = Header;
